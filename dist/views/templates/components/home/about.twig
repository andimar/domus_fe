<section class="about stripe_section">
    <div class="boxed">
        <div class="section-title">
            <h3>{{about.title}}</h3>
        </div> 
        <p>{{about.text}}</p>
        <div class="buttons more">
            <a href="{{about.btn1.url}}" class="btn secondary">{{about.btn1.cta}}</a>
            <a href="{{about.btn2.url}}" class="btn primary">{{about.btn2.cta}}</a>
        </div>
    </div>
</section>
